apiVersion: v1
kind: Service
metadata:
  labels:
    app: notification-system-audit
    environment: Development
  name: notification-system-audit
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: notification-system-audit
  sessionAffinity: None
  type: ClusterIP
